University assignment to create a static analysis tool.

This tool uses Roslyn analyzers to create a rule that you cannot inject a DbContext to a ASP.NET Controller. Pretty arbitrary, but so was the assignment :D 
